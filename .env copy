spring.profiles.active=r2

# R2 / Cloud storage
r2.accountId=
r2.accessKeyId=
r2.secretAccessKey=
r2.bucket=hls
r2.endpoint=
HLS_ENCRYPTION_ENABLED=true

# CDN
cdn.baseUrl=

# Mongo
MONGODB_URI=mongodb://localhost:27017/m3u8

CORS_ALLOWED_ORIGINS=*
JWT_SECRET=
JWT_EXPIRATION_MINUTES=15
JWT_BUFFER_MINUTES=30
SERVER_PORT=8080
# WARNING : SERVER_HOST must match SERVER_PORT and should be host:port only (no path)
SERVER_HOST=localhost:8080

protocol=http